<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Overpayment_Protection</fullName>
    <active>true</active>
    <description>Prevents manual edits to payment amount unless BYPASS__c is checked; totals are enforced in Apex</description>
    <errorConditionFormula>AND(NOT(ISNEW()), ISCHANGED(Payment_Amount__c), NOT(BYPASS__c))</errorConditionFormula>
    <errorMessage>Payment amount cannot be edited unless BYPASS__c is checked.</errorMessage>
</ValidationRule>
